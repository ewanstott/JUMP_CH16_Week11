// MAP - creates a new array using values from the original array.
// Original array
// const numbers = [1, 2, 3, 4, 5];

// Using .map() to create a new array where each element is doubled
// const doubledNumbers = numbers.map((num) => num * 2);

// console.log("Original array:", numbers);
// console.log("Doubled array:", doubledNumbers);

const arr = [1, 2, 3];
// PUSH - add new items to end of array
// const x = arr.push(5);
// console.log(arr);

// Shift - remove first element of array and return it
const x = arr.shift();

console.log([1, 2, 3].reduce((x, y) => x * y));
